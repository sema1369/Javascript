// 4 - LES TYPES DE DONNEES - SEMA

/* ======================================== A - LES TYPES PRIMITIF ======================================= */

let prenom = 'Sema'; // 'Sema' = string (une chaine de caractère)
console.log(prenom, typeof prenom);
// typeof permet de verifier le type de donnée de notre variable. 

let age = 27; // 27 = number (un nombre)
console.log(age, typeof age);

let estConnecte = true; // true = boolean (un boolean peut être vrai (true) ou faux (false))
console.log(estApprouve, typeof estApprouve);

let nom = undefined; // undefined = n'est pas définie. 
console.log(nom, typeof nom);

let couleurSelectionne = null; // null = vide (pas de réponse) 
console.log(couleurSelectionne, typeof couleurSelectionne);

