// 11 - OPERATEUR TERNAIRE - SEMA

// Exemple : 
// Si un client a plus de 100 points :
// Il est un client en 'or', sinon,
// Il est un client en 'argent'. 

let pointsClientUn = 110; 
let type = pointsClientUn > 100 ? 'gold' : 'silver';
console.log(type);

let pointsClientDeux = 90;
type = pointsClientDeux > 100 ? 'gold' : 'silver'; 
console.log(type);



